export const Header = () => {
  return (
    <header>
      <nav>
        <div>
          <div>
            {/* Authentication removed: No sign-in or user info */}
          </div>
        </div>
      </nav>
    </header>
  )
}